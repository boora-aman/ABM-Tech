Drop product screenshots here and reference them from
`src/lib/content/showcase.ts` via the `image` field, e.g. `/showcase/crm.webp`.

Recommended: 1600×1000 (16:10), WebP or AVIF, under 250 KB.
Until a slide has an image it renders a labelled placeholder frame.
